import { CartItem } from '../../common/types';
import { CartState, IncrementItemAction } from '~/hooks/use-cart/use-cart.types';
declare const incrementItem: IncrementItemFunction;
export default incrementItem;
export declare type IncrementItemFunction = (state: CartState, action: IncrementItemAction) => {
    cart: CartItem[];
    show: boolean;
};
