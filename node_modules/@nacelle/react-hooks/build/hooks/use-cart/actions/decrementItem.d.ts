import { CartItem } from '../../common/types';
import { CartState, DecrementItemAction } from '~/hooks/use-cart/use-cart.types';
declare const decrementItem: DecrementItemFunction;
export default decrementItem;
export declare type DecrementItemFunction = (state: CartState, action: DecrementItemAction) => {
    cart: CartItem[];
    show: boolean;
};
