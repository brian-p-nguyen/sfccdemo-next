import { CartItem } from '../../common/types';
import { InitCartAction, CartState } from '~/hooks/use-cart/use-cart.types';
declare const initCart: InitCartFunction;
export default initCart;
export declare type InitCartFunction = (state: CartState, action: InitCartAction) => {
    cart: CartItem[];
    show: boolean;
};
