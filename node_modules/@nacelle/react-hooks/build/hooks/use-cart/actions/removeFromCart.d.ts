import { CartItem } from '../../common/types';
import { CartState, RemoveFromCartAction } from '~/hooks/use-cart/use-cart.types';
declare const removeFromCart: RemoveFromCartFunction;
export default removeFromCart;
export declare type RemoveFromCartFunction = (state: CartState, action: RemoveFromCartAction) => {
    cart: CartItem[];
    show: boolean;
};
