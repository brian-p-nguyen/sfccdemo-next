import { CartState, ClearCartAction } from '~/hooks/use-cart/use-cart.types';
declare const clearCart: ClearCartFunction;
export default clearCart;
export declare type ClearCartFunction = (state: CartState, action: ClearCartAction) => {
    cart: any[];
    show: boolean;
};
