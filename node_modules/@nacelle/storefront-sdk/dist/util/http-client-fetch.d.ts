import { HttpClient, HttpClientResponse } from '../types/http';
export interface FetchResponse {
    headers: Headers;
    ok: boolean;
    redirected: boolean;
    status: number;
    statusText: string;
    type: ResponseType;
    url: string;
}
export declare class HttpClientFetch implements HttpClient {
    options: RequestInit;
    constructor(options?: RequestInit);
    get<T>(url: string): Promise<HttpClientResponse<T>>;
    post<T>(url: string, data: unknown): Promise<HttpClientResponse<T>>;
}
